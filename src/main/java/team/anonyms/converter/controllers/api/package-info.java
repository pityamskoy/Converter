/**
 * <p>
 *     This package only includes controllers, designed to work with remote microservices,
 *     which send requests directly to this application (Converter monolith backend).
 * </p>
 */
package team.anonyms.converter.controllers.api;